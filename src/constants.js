// Available User Register Methods
export const REGISTER_METHODS = {
  EMAIL_PASSWORD: 'email_password',
  GOOGLE: 'google',
};

export const REGISTER_METHOD_ENUM = Object.values(REGISTER_METHODS);

// Available User Roles
export const USER_ROLES = {
  USER: 'user',
  ADMIN: 'admin',
};

export const USER_ROLE_ENUM = Object.values(USER_ROLES);
